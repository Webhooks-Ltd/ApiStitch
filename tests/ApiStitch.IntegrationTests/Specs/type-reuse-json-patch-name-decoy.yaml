openapi: 3.0.3
info:
  title: Type Reuse JsonPatch Name Decoy API
  version: 1.0.0
paths:
  /decoys:
    post:
      operationId: createDecoy
      tags: [Decoys]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/JsonPatchNameDecoy'
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JsonPatchNameDecoy'
components:
  schemas:
    JsonPatchNameDecoy:
      type: object
      x-apistitch-type: SampleApi.Models.JsonPatchDocumentWrapper
      properties:
        id:
          type: integer
          format: int64
